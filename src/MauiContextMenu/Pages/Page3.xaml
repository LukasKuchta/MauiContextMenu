<?xml version="1.0" encoding="utf-8" ?>
<page:ContentPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Page3"
             xmlns:page="using:MauiContextMenu.Pages.Base"
                 xmlns:local="clr-namespace:MauiContextMenu"
             xmlns:vm="clr-namespace:MauiContextMenu.ViewModels.Page"
             Shell.NavBarIsVisible="False"
             ControlTemplate="{x:StaticResource ContextMenuPageTemplate}"
             x:DataType="vm:Page3ViewModel"
             Title="{Binding Title}">
    <Grid>
        <Label
            Text="Add another content here!"
            VerticalOptions="Center"
            HorizontalOptions="Center" />
    </Grid>
</page:ContentPageBase>