<?xml version="1.0" encoding="utf-8" ?>
<page:ContentPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="using:MauiContextMenu"
             xmlns:vm="using:MauiContextMenu.ViewModels.Page"
             xmlns:page="using:MauiContextMenu.Pages.Base"
             x:Class="MauiApp1.Page1"
             Shell.NavBarIsVisible="False"
             ControlTemplate="{x:StaticResource ContextMenuPageTemplate}"
             x:DataType="vm:Page1ViewModel"
             Title="{Binding Title}">

    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <Label Text="Add content here." />
    </VerticalStackLayout>

</page:ContentPageBase>